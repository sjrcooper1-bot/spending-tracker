import { useState } from 'react';

// Placeholder — to be built out phase by phase
export default function App() {
  return (
    <div className="min-h-screen bg-gray-50 flex flex-col items-center justify-center">
      <h1 className="text-3xl font-semibold text-gray-800">Spending Analyser</h1>
      <p className="mt-2 text-gray-500">Upload your bank statements to get started.</p>
    </div>
  );
}
